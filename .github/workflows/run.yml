on:
  push:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v3
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
      - run: node -v
